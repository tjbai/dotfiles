When a task needs TJ's work desktop `work` (files, credentials, or processes on that machine), follow the `work` runner rule in the global agent guidance: `list_runners`, then `create_thread` on runner `work` with a tight task and ask for a reply; if the runner is absent, say the desktop is offline and continue.
